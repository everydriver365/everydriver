# Instructor Mobile Home — 6 Redesign Concepts (Demo Page)\n\nGoal: Build a single demo route that renders 6 self-contained redesign variants of the instructor mobile home, all preserving current functionality (greeting/header, next lesson / up-next, today's schedule, week stats, quick actions / pinned tiles, open slots, alerts/notifications, add lesson, fill gaps, settings entry). Style: iOS light mode, professional, restrained color use.\n\n## Demo route\n\n- New page: `src/pages/InstructorHomeDesignLab.tsx`\n- Route: `/instructor/home-lab` (added to `src/routes/instructorAppRoutes.tsx`, mobile-only preview)\n- UI: Top sticky variant switcher (segmented control: V1–V6) + a phone-frame container (440px) rendering the selected variant. Each variant lives in its own file under `src/components/instructor/homeLab/`.\n- All variants consume the **same mock data object** (no live queries) so the focus is purely visual. A small header note clarifies \