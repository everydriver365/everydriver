## Why the end-of-lesson summary doesn't appear\n\n`InstructorLiveSession.tsx → stopSession()` is supposed to open `TripSummarySheet` after the user holds the red **End** button, but the summary is gated on a long chain of awaits that runs **before** the sheet is opened (lines 990–1149):\n\n```text\nEnd held →\n  fetch GPS points (await)\n  update lesson_telematics  (await)\n  fetch session row         (await)\n  insert saved_routes        (await)   ← throws if RLS / null FK / dup key\n  autoCaptureLessonRoute()   (await)   ← throws if route too short\n  update live_pupil_positions(await)\n  update gps_devices         (await)   ← throws on stale session\n  setCompletedSessionId(id)            ← only reached on full success\n  setShowReport(true)\n  navigate(\