I found two separate problems to address:\n\n1. Email/password login was failing before the app could do anything useful: the hosted auth endpoint was returning 500/504 timeouts while looking up the user. The backend is healthy now, and I have turned off the leaked-password/HIBP check that can add a long external password check during sign-in.\n2. The TestFlight Face ID path auto-starts as soon as the login screen loads, then calls the same email/password sign-in behind the scenes. If auth is slow or the native biometric prompt hangs, the UI can sit on the spinner without a clear recovery path.\n\nDo I know what the issue is? Yes: email/password was being blocked by backend auth timeout behaviour, and Face ID has no timeout/cancel fallback around the native biometric + saved-credential sign-in flow.\n\nPlan:\n\n- Update `src/pages/instructor-app/InstructorLogin.tsx`\n  - Stop auto-running Face ID immediately on page load in TestFlight.\n  - Only show the Face ID/Quick Sign In button when