## Schedule tile — shrink icon 15%

Scope: `src/components/instructor/ScheduleTile.tsx` (the Schedule card on the instructor mobile home).

Change the empty-state `CalendarOff` icon from `size={28}` → `size={24}` (28 × 0.85 ≈ 23.8, rounded to 24). Nothing else on the tile changes.

If you actually meant a different icon (chevron on each lesson row, the small map pin, or the +/refresh icons in the footer buttons), tell me which and I'll resize that one instead.