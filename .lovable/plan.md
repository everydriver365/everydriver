

Change the title text on the courses search page from its current value to "Find a Course".

**File: `src/components/courses/CourseSearchHeader.tsx`**
- The `title` prop is passed in from the parent. Need to check where it's called.

**File: parent of CourseSearchHeader**
- Find where `CourseSearchHeader` is used and update the `title` prop to `"Find a Course"`.

Let me check the parent.

