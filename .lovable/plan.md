\n## Goal\n\nMake `winchesterdrivingschool.co.uk` (and `www.`) behave as a complete clone of `drive365.co.uk` — same pages, same layouts, same booking/login flows — but white-labelled as **Winchester Driving School** and scoped so all course search, availability and bookings only return Ken-D's data. Pupils log in directly on the domain at `/pupil/login` and stay on the domain after login.\n\nIt currently redirects to Ken-D's mini-website (Polaroid Stack). This plan replaces that behaviour.\n\n## What changes\n\n### 1. Domain routing (`src/components/DomainRouter.tsx`)\n- Remove `winchesterdrivingschool.co.uk` from `CUSTOM_DOMAIN_SLUGS` (so it stops being treated as an instructor mini-website subdomain).\n- Add a new `WHITELABEL_DOMAINS` map:\n  ```ts\n  { \