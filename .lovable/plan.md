## Goal\n\nWhen a visitor is on the **Winchester Driving School** whitelabel domain (`winchesterdrivingschool.co.uk` / `www.winchesterdrivingschool.co.uk`), repaint the brand blue to **`#08507f`** — header, mobile bottom nav, footer, and primary blue accents (buttons, links, highlights).\n\nToday these surfaces use the shared `--primary` token, so they show the default Drive365 navy. Footer was already pinned to `#09507f` globally; we'll move that to whitelabel-aware too so Winchester gets the new shade and the main Drive365 sites are unaffected.\n\n## Approach\n\nScope the colour change to the whitelabel domain only by overriding the CSS token at the root when `isWhitelabelDomain()` is true. This is a one-line change that cascades to every component using `bg-primary`, `text-primary`, `--primary`, etc. — no per-component edits needed.\n\n## Steps\n\n1. **Inject a whitelabel CSS variable override** (in `src/main.tsx` or a small `WhitelabelTheme` component mounted by `App.tsx`):\n   - On mount, if `getWhitelabelConfig()` returns Winchester, set on `document.documentElement`:\n     - `--primary: 202 89% 26%;` (HSL of `#08507f`)\n     - `--ring: 202 89% 26%;`\n   - This automatically restyles `<Header>` (`bg-primary`), `<MobileBottomNav>` (`bg-primary`), buttons, focus rings, links etc.\n\n2. **Footer** — replace the hard-coded inline `style={{ backgroundColor: \