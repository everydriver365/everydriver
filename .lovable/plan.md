## Goal\n\nOn `winchesterdrivingschool.co.uk`, give every page the **same** bottom nav, make every link land on a real branded page (no 404s / blank screens), and make the `/courses` page actually show Ken D's upcoming availability instead of an empty state.\n\n## Problems found\n\n1. **Inconsistent nav.** The home page (`MobileHomepage`) renders one nav pointing at `/i/ken-d/*` (mini-website routes). The mini-website pages render a *different* nav (`MiniWebsiteMobileBottomNav`) whose \