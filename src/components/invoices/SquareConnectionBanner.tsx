// Square removed. Stub retained to satisfy existing imports. Returns null so
// no banner is displayed — Ryft onboarding lives in the Payments tab.
export function SquareConnectionBanner(_: { instructorId?: string }) {
  return null;
}
